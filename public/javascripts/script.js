document.addEventListener('DOMContentLoaded', () => {

  console.log('IronGenerator JS imported successfully!');

}, false);



function createPlayerArray(Position){
  if (createPlayerArray.Position === Position)

  Position.push(Player);
}

function randomPlayer(Position, FPTS){
  const tempPlayer = Position[Math.floor(Math.random)* Position.length];
  if (tempPlayer.FPTS >= [i] && tempPlayer.Position === [i]){
  return tempPlayer;
  }else {
    randomPlayer(Position, FPTS);
} 

}


function Draft() {
  const draftTeam = [];
  const playerPosition = ["QB","RB","WR","TE","DST","K"];
  const playerFPTS = [];
  



}